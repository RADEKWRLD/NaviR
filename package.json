{
  "dependencies": {
    "gsap": "^3.13.0",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "wrangler": "^4.16.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "host": "vite --host",
    "deploy": "npm run build && wrangler deploy"
  }
}
